--- pyproject.toml.orig	2026-07-09 05:28:29 UTC
+++ pyproject.toml
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["maturin>=1.2,<1.14"]
+requires = ["maturin>=1.2"]
 build-backend = "maturin"
 
 [project]
