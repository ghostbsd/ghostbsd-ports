A GNUstep theme based on OS X Maverick.  The main theme purpose is to demonstrate
that GNUstep is themable, GNUstep can look modern, GNUstep is not OPENSTEP it
follows Cocoa, and it is a Cocoa free and open source reimplementation.
