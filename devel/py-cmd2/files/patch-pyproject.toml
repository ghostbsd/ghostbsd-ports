--- pyproject.toml.orig	2026-06-05 13:24:55 UTC
+++ pyproject.toml
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["build>=1.3.0", "setuptools>=80.8.0", "setuptools-scm>=9.2.1"]
+requires = ["setuptools>=61", "setuptools-scm>=9.2.1"]
 build-backend = "setuptools.build_meta"
 
 [project]
@@ -10,8 +10,7 @@ keywords = ["CLI", "cmd", "command", "interactive", "p
 readme = "README.md"
 requires-python = ">=3.11"
 keywords = ["CLI", "cmd", "command", "interactive", "prompt", "Python"]
-license = "MIT"
-license-files = ["LICENSE"]
+license = { file = "LICENSE" }
 classifiers = [
     "Development Status :: 5 - Production/Stable",
     "Environment :: Console",
