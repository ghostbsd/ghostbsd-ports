--- Makefile.orig	2026-06-23 16:23:30 UTC
+++ Makefile
@@ -23,7 +23,7 @@ endif
 ifeq ($(WANT_CRASH_REPORTS),1)
 QMAKE_FLAGS+=IAITO_ENABLE_CRASH_REPORTS=true
 endif
-QMAKE_FLAGS+=PREFIX=$(PREFIX)
+#QMAKE_FLAGS+=PREFIX=$(PREFIX)
 INDENT_IMAGE_NAME ?= clang-format-image
 INDENT_IMAGE_TAG ?= llvm20
 INDENT_IMAGE := $(INDENT_IMAGE_NAME):$(INDENT_IMAGE_TAG)
